apiVersion: apps/v1
kind: Deployment
metadata:
  name: question-service
  labels:
    app: question-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: question-service
  template:
    metadata:
      labels:
        app: question-service
    spec:
      containers:
        - name: question-service
          image: sylse/question-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: db_root_password
              valueFrom:
                secretKeyRef:
                  name: mso-secrets
                  key: db_root_password
            - name: db_user
              value: root

---
apiVersion: v1
kind: Service
metadata:
  name: question-service
  labels:
    app: question-service
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: question-service
  type: ClusterIP
